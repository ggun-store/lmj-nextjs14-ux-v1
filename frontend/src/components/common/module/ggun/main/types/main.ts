export type CenterTitle = {
    title : string;
}
export type leftBot = {
    title : string;
}
export type rightBot = {
    title : string;
}
export type botcom = {
    title : string;
    src : string;
}